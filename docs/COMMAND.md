Gally is a command line tool. To know more about the associated commands, see:

- Use `gally help` to get the various gally options
- Use `gally help [command]` or `gally [command] --help` for more information
  about a command.

builds:
  - binary: gally
    goos:
      - darwin
      - linux
    goarch:
      - amd64
    env:
      - CGO_ENABLED=0
archive:
  format: tar.gz
  replacements:
    amd64: 64-bit
    darwin: macOS
